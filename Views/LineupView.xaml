<UserControl x:Class="Summit.Views.LineupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:h="clr-namespace:Summit.Helpers"
             Background="Transparent" Opacity="0">

    <UserControl.Resources>
        <h:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
        <h:BoolToBrushConverter x:Key="SelectedBorderBrush" TrueBrush="#60FF6A1A" FalseBrush="#1E232E"/>
        <h:BoolToBrushConverter x:Key="SelectedBackground" TrueBrush="#171B22" FalseBrush="#12151B"/>
    </UserControl.Resources>

    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.22"/>
                    <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                                     From="12" To="0" Duration="0:0:0.22">
                        <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>
    <UserControl.RenderTransform><TranslateTransform/></UserControl.RenderTransform>

    <ScrollViewer>
        <Grid Margin="40,28,40,40">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="12"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="6"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="18"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Back -->
            <Button Grid.Row="0" HorizontalAlignment="Left" Background="Transparent" BorderThickness="0"
                    Cursor="Hand" Command="{Binding BackCommand}">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <StackPanel Orientation="Horizontal" Background="Transparent">
                            <TextBlock Text="&#xE72B;" FontFamily="Segoe MDL2 Assets" FontSize="12"
                                       Foreground="#9AA5B5" Margin="0,0,6,0" VerticalAlignment="Center"/>
                            <TextBlock Text="VOLTAR" FontFamily="Segoe UI" FontWeight="Bold"
                                       FontSize="10" Foreground="#9AA5B5" VerticalAlignment="Center"/>
                        </StackPanel>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <TextBlock Grid.Row="2" Text="ESCALAÇÃO" FontFamily="Segoe UI" FontWeight="Black"
                       FontSize="28" Foreground="{StaticResource BrushTextPrimary}"/>
            <TextBlock Grid.Row="4" Text="Escolha os jogadores e o capitão da escalação. Clique num jogador pra selecionar; clique na coroa pra definir o capitão."
                       FontFamily="Segoe UI" FontSize="12" Foreground="#9AA5B5" TextWrapping="Wrap"/>

            <StackPanel Grid.Row="6">
                <ItemsControl ItemsSource="{Binding Members}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,0,8" Padding="16,12" CornerRadius="8" Cursor="Hand"
                                    BorderThickness="1"
                                    Background="{Binding IsSelected, Converter={StaticResource SelectedBackground}}"
                                    BorderBrush="{Binding IsSelected, Converter={StaticResource SelectedBorderBrush}}">
                                <Border.InputBindings>
                                    <MouseBinding MouseAction="LeftClick"
                                                  Command="{Binding DataContext.ToggleSelectCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                  CommandParameter="{Binding User.Id}"/>
                                </Border.InputBindings>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Grid.Column="0" Width="36" Height="36" CornerRadius="18" Margin="0,0,14,0">
                                        <Border.Background>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                <GradientStop Color="#262C39" Offset="0"/>
                                                <GradientStop Color="#1D222C" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Border.Background>
                                        <TextBlock Text="{Binding User.Nickname[0]}" FontFamily="Segoe UI"
                                                   FontWeight="Black" FontSize="14" Foreground="#9AA5B5"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding User.Nickname}" FontFamily="Segoe UI" FontWeight="Bold"
                                                   FontSize="14" Foreground="{StaticResource BrushTextPrimary}"/>
                                        <TextBlock Text="{Binding User.PrimaryRole}" FontFamily="Segoe UI" FontSize="11"
                                                   Foreground="#9AA5B5" Margin="0,2,0,0"/>
                                    </StackPanel>

                                    <Button Grid.Column="2" Background="Transparent" BorderThickness="0"
                                            Padding="8,0" Cursor="Hand" ToolTip="Definir capitão da escalação"
                                            Command="{Binding DataContext.SetCaptainCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding User.Id}"
                                            Visibility="{Binding IsSelected, Converter={StaticResource BoolToVisibility}}">
                                        <TextBlock Text="&#xE734;" FontFamily="Segoe MDL2 Assets" FontSize="16">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#3A414F"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsCaptainChoice}" Value="True">
                                                            <Setter Property="Foreground" Value="#FFD700"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Button>

                                    <TextBlock Grid.Column="3" Text="&#xE73E;" FontFamily="Segoe MDL2 Assets"
                                               FontSize="16" Foreground="#22C55E" VerticalAlignment="Center"
                                               Visibility="{Binding IsSelected, Converter={StaticResource BoolToVisibility}}"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>

            <StackPanel Grid.Row="8">
                <TextBlock Text="{Binding SelectedCountLabel}" FontFamily="Segoe UI" FontWeight="Bold"
                           FontSize="11" Foreground="#9AA5B5" Margin="0,0,0,10"/>
                <StackPanel Orientation="Horizontal">
                    <Button Content="SALVAR ESCALAÇÃO" Style="{StaticResource PrimaryButton}"
                            Command="{Binding SaveCommand}" Margin="0,0,12,0"/>
                    <TextBlock Text="{Binding Message}" FontFamily="Segoe UI" FontSize="12"
                               Foreground="#9AA5B5" VerticalAlignment="Center"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
