<Window x:Class="Summit.Views.SplashView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Summit" Width="520" Height="320"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" ResizeMode="NoResize"
        AllowsTransparency="True" Background="Transparent">

    <Border CornerRadius="16" ClipToBounds="True">
        <Grid>
            <!-- BG -->
            <Rectangle>
                <Rectangle.Fill>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#0E0E10" Offset="0"/>
                        <GradientStop Color="#180A04" Offset="0.5"/>
                        <GradientStop Color="#0E0E10" Offset="1"/>
                    </LinearGradientBrush>
                </Rectangle.Fill>
            </Rectangle>

            <!-- Glow -->
            <Ellipse Width="500" Height="500" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Ellipse.Fill>
                    <RadialGradientBrush>
                        <GradientStop Color="#14FF4D00" Offset="0"/>
                        <GradientStop Color="#00FF4D00" Offset="1"/>
                    </RadialGradientBrush>
                </Ellipse.Fill>
            </Ellipse>

            <!-- Content -->
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">

                <!-- Logo -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center"
                            x:Name="LogoPanel" Opacity="0">
                    <StackPanel.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                     From="0" To="1" Duration="0:0:0.6"
                                                     BeginTime="0:0:0.2"/>
                                    <DoubleAnimation Storyboard.TargetName="LogoPanel"
                                                     Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                     From="0.85" To="1" Duration="0:0:0.6"
                                                     BeginTime="0:0:0.2">
                                        <DoubleAnimation.EasingFunction>
                                            <CubicEase EasingMode="EaseOut"/>
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                    <DoubleAnimation Storyboard.TargetName="LogoPanel"
                                                     Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                     From="0.85" To="1" Duration="0:0:0.6"
                                                     BeginTime="0:0:0.2">
                                        <DoubleAnimation.EasingFunction>
                                            <CubicEase EasingMode="EaseOut"/>
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </StackPanel.Triggers>
                    <StackPanel.RenderTransform>
                        <ScaleTransform/>
                    </StackPanel.RenderTransform>
                    <StackPanel.RenderTransformOrigin>0.5,0.5</StackPanel.RenderTransformOrigin>

                    <Border Width="6" Height="52" CornerRadius="3" Margin="0,0,14,0">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#FF6A00" Offset="0"/>
                                <GradientStop Color="#D7261E" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <Border.Effect>
                            <DropShadowEffect Color="#FF6A00" BlurRadius="20" ShadowDepth="0" Opacity="0.7"/>
                        </Border.Effect>
                    </Border>
                    <TextBlock Text="SUMMIT" FontFamily="Segoe UI" FontWeight="Black" FontSize="52"
                               Foreground="#F5F5F5" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Tagline -->
                <TextBlock Text="A ARENA DOS MELHORES" FontFamily="Segoe UI" FontWeight="SemiBold"
                           FontSize="11" Foreground="#55555F" HorizontalAlignment="Center"
                           Margin="0,10,0,36" x:Name="Tagline" Opacity="0">
                    <TextBlock.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                     From="0" To="1" Duration="0:0:0.5"
                                                     BeginTime="0:0:0.6"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </TextBlock.Triggers>
                </TextBlock>

                <!-- Loading bar container -->
                <Border Width="220" Height="2" CornerRadius="1" Background="#1E1E25"
                        x:Name="BarContainer" Opacity="0">
                    <Border.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                     From="0" To="1" Duration="0:0:0.3"
                                                     BeginTime="0:0:0.8"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Border.Triggers>
                    <Border x:Name="LoadingBar" HorizontalAlignment="Left" CornerRadius="1"
                            Width="0" Height="2">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#FF6A00" Offset="0"/>
                                <GradientStop Color="#D7261E" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <Border.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Width"
                                                         From="0" To="220" Duration="0:0:1.4"
                                                         BeginTime="0:0:0.8">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseInOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Border.Triggers>
                    </Border>
                </Border>

                <!-- Version -->
                <TextBlock Text="v0.1.0 MVP" FontFamily="Consolas" FontSize="9"
                           Foreground="#2A2A35" HorizontalAlignment="Center" Margin="0,12,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
