<Window x:Class="Summit.Views.SplashView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Summit" Width="520" Height="320"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" ResizeMode="NoResize"
        AllowsTransparency="True" Background="Transparent">

    <Border CornerRadius="16" ClipToBounds="True">
        <Grid>
            <!-- BG -->
            <Rectangle>
                <Rectangle.Fill>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#0A0C10" Offset="0"/>
                        <GradientStop Color="#180A04" Offset="0.5"/>
                        <GradientStop Color="#0A0C10" Offset="1"/>
                    </LinearGradientBrush>
                </Rectangle.Fill>
            </Rectangle>

            <!-- Glow -->
            <Ellipse Width="500" Height="500" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Ellipse.Fill>
                    <RadialGradientBrush>
                        <GradientStop Color="#14FF5024" Offset="0"/>
                        <GradientStop Color="#00FF5024" Offset="1"/>
                    </RadialGradientBrush>
                </Ellipse.Fill>
            </Ellipse>

            <!-- Content -->
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">

                <!-- Logo -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center"
                            x:Name="LogoPanel" Opacity="0">
                    <StackPanel.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                     From="0" To="1" Duration="0:0:0.6"
                                                     BeginTime="0:0:0.2"/>
                                    <DoubleAnimation Storyboard.TargetName="LogoPanel"
                                                     Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                     From="0.85" To="1" Duration="0:0:0.6"
                                                     BeginTime="0:0:0.2">
                                        <DoubleAnimation.EasingFunction>
                                            <CubicEase EasingMode="EaseOut"/>
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                    <DoubleAnimation Storyboard.TargetName="LogoPanel"
                                                     Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                     From="0.85" To="1" Duration="0:0:0.6"
                                                     BeginTime="0:0:0.2">
                                        <DoubleAnimation.EasingFunction>
                                            <CubicEase EasingMode="EaseOut"/>
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </StackPanel.Triggers>
                    <StackPanel.RenderTransform>
                        <ScaleTransform/>
                    </StackPanel.RenderTransform>
                    <StackPanel.RenderTransformOrigin>0.5,0.5</StackPanel.RenderTransformOrigin>

                    <Image Source="/Assets/logo.png" Height="66" Margin="0,0,16,0"
                           VerticalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality">
                        <Image.Effect>
                            <DropShadowEffect Color="#FF3B30" BlurRadius="28" ShadowDepth="0" Opacity="0.7"/>
                        </Image.Effect>
                    </Image>
                    <TextBlock Text="SUMMIT" FontFamily="Segoe UI" FontWeight="Black" FontSize="52"
                               Foreground="#F2F5FA" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Tagline -->
                <TextBlock FontFamily="Segoe UI" FontWeight="Bold"
                           FontSize="10" HorizontalAlignment="Center"
                           Margin="0,10,0,36" x:Name="Tagline" Opacity="0">
                    <Run Text="FOCO" Foreground="#9AA5B5"/><Run Text="  //  " Foreground="#FF3B30"/><Run Text="PRECISÃO" Foreground="#9AA5B5"/><Run Text="  //  " Foreground="#FF3B30"/><Run Text="DOMINAÇÃO" Foreground="#9AA5B5"/>
                    <TextBlock.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                     From="0" To="1" Duration="0:0:0.5"
                                                     BeginTime="0:0:0.6"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </TextBlock.Triggers>
                </TextBlock>

                <!-- Loading bar container -->
                <Border Width="220" Height="2" CornerRadius="1" Background="#181D26"
                        x:Name="BarContainer" Opacity="0">
                    <Border.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                     From="0" To="1" Duration="0:0:0.3"
                                                     BeginTime="0:0:0.8"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Border.Triggers>
                    <Border x:Name="LoadingBar" HorizontalAlignment="Left" CornerRadius="1"
                            Width="0" Height="2">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#FF6A1A" Offset="0"/>
                                <GradientStop Color="#FF3B30" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <Border.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Width"
                                                         From="0" To="220" Duration="0:0:1.4"
                                                         BeginTime="0:0:0.8">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseInOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Border.Triggers>
                    </Border>
                </Border>

                <!-- Version -->
                <TextBlock Text="SUMMIT ALPHA · v0.2" FontFamily="Consolas" FontSize="9"
                           Foreground="#262C39" HorizontalAlignment="Center" Margin="0,12,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
